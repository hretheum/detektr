# Production override - używa obrazów z GitHub Container Registry
# Używane na Nebuli zamiast lokalnych obrazów

services:
  example-otel:
    image: ${REGISTRY:-ghcr.io}/${IMAGE_PREFIX:-hretheum/detektr}/example-otel:${TAG:-latest}

  frame-tracking:
    image: ${REGISTRY:-ghcr.io}/${IMAGE_PREFIX:-hretheum/detektr}/frame-tracking:${TAG:-latest}

  echo-service:
    image: ${REGISTRY:-ghcr.io}/${IMAGE_PREFIX:-hretheum/detektr}/echo-service:${TAG:-latest}

  rtsp-capture:
    image: ${REGISTRY:-ghcr.io}/${IMAGE_PREFIX:-hretheum/detektr}/rtsp-capture:${TAG:-latest}

  frame-buffer:
    image: ${REGISTRY:-ghcr.io}/${IMAGE_PREFIX:-hretheum/detektr}/frame-buffer:${TAG:-latest}

  telegram-alerts:
    image: ${REGISTRY:-ghcr.io}/${IMAGE_PREFIX:-hretheum/detektr}/telegram-alerts:${TAG:-latest}

  gpu-demo:
    image: ${REGISTRY:-ghcr.io}/${IMAGE_PREFIX:-hretheum/detektr}/gpu-demo:${TAG:-latest}
