# Override dla lokalnego developmentu
# W produkcji używamy obrazów z registry

services:
  example-otel:
    build:
      context: .
      dockerfile: ./services/example-otel/Dockerfile
    image: example-otel:simple

  frame-tracking:
    build:
      context: .
      dockerfile: ./services/frame-tracking/Dockerfile
    image: frame-tracking:latest

  echo-service:
    build:
      context: .
      dockerfile: ./services/echo-service/Dockerfile
    image: echo-service:latest

  rtsp-capture:
    build:
      context: .
      dockerfile: ./services/rtsp-capture/Dockerfile
    image: rtsp-capture:latest

  frame-buffer:
    build:
      context: .
      dockerfile: ./services/frame-buffer/Dockerfile
    image: frame-buffer:latest

  telegram-alerts:
    build:
      context: .
      dockerfile: ./services/telegram-alerts/Dockerfile
    image: telegram-alerts:latest

  gpu-demo:
    build:
      context: .
      dockerfile: ./services/gpu-demo/Dockerfile
    image: gpu-demo:latest
